import React, { useEffect, useState } from 'react';
import { ApiResponse, TransferRequest } from '@/types/common';

const TransferRequests: React.FC = () => {
  // ... component code remains the same
};

export default TransferRequests; 