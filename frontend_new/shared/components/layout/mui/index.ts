/**
 * MUI layout component exports
 */

// Re-export MUI components
export { Box, Container, Grid, Stack } from '@mui/material';

// Export custom MUI components
export { AppBarContent } from './AppBarContent';
export { Spacer } from './Spacer';

// Export types
export type {
  BoxProps,
  ContainerProps,
  GridProps,
  StackProps
} from '@mui/material';
