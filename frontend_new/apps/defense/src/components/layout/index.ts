/**
 * Defense-specific layout components
 */

// Export with defense-specific names
export { default as DefenseLayout } from './Layout';
export { default as DefenseSidebar } from './Sidebar';

// Re-export with original names for backward compatibility
export { default as Layout } from './Layout';
export { default as Sidebar } from './Sidebar';
