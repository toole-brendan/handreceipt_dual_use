import { FC } from 'react';
import { Box, Typography } from '@mui/material';

const AlertsPage: FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Alerts & Tasks</Typography>
    </Box>
  );
};

export default AlertsPage; 